# 2026-09-09

## 考公学习站（d:/C/考公）技术栈确认与宝塔部署改造
- 技术栈：React 19 + vinext（OpenAI Sites 生成的类 Next RSC 框架）+ Vite 8 + Tailwind 4 + shadcn 风格组件；数据库 Cloudflare D1（代码直接用 env.DB 原生 API），专为 Cloudflare Workers 构建。
- 问题：`cloudflare:workers` 模块 + Worker 构建产物，无法直接跑在宝塔 Node 环境。
- 改造（已完成）：
  - 新增 `server/prod.mjs`：用 Miniflare（workerd）在服务器运行 dist/server 的 Worker，D1 持久化到 `.wrangler/state/v3`，首次启动自动应用 `drizzle/0000_fuzzy_onslaught.sql` 迁移。
  - `package.json` 加 `start:prod` 脚本和 devDependency `miniflare ^4.0.0`。
  - `lib/server-db.ts` 的 safeRequest 从比较完整 origin 改为比较 host（解决 Nginx 反代后 http/https 协议不一致误报跨域）。
- 部署路径：宝塔 Node ≥22.13 → npm ci → npm run build → PM2 跑 server/prod.mjs（端口 3000）→ Nginx 反代 + SSL。
- 备选方案（未采用）：直接部署到 Cloudflare（零改动）；或迁移 Next.js + better-sqlite3（工作量大）。
